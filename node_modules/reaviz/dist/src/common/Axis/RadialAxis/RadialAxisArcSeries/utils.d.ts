/**
 * Given a set of arcs, return an array of points for each arc.
 */
export declare const getPointsForLevels: ({ count, outerRadius, ticks, arcs }: {
    count: any;
    outerRadius: any;
    ticks: any;
    arcs: any;
}) => {
    x: number;
    y: number;
}[][];
