import React, { FC, Ref } from 'react';
export interface PageTitleProps extends React.HTMLAttributes<HTMLHeadingElement> {
    /**
     * Color variation of the title.
     */
    color?: 'default' | 'primary' | 'secondary' | 'error' | 'success' | 'warning' | 'info';
    /**
     * Font variant for the title.
     */
    variant?: 'default' | 'mono';
    /**
     * Whether to disable the margins.
     */
    disableMargins?: boolean;
}
export interface PageTitleRef {
    ref?: Ref<HTMLHeadingElement>;
}
export declare const PageTitle: FC<PageTitleProps & PageTitleRef>;
